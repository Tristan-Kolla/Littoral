# littoral.ode

# Helper ---------------------------------------------------------------------------

maturation = (phi*J)/(1+J*J)
logistic = r*For - b*For*For
type3 = (h*P*For*For)/(1+kappa*For*For)

# Equations ------------------------------------------------------------------------

dP/dt = maturation - (mP+mu)*P
dFor/dt = logistic - type3 + eps*For*J
dJ/dt = f*P - maturation - mJ*J - k*For*J - c*P*J

# Parameters -----------------------------------------------------------------------

param mu=0.00, mP=0.280, mJ=0.280 
param r=0.500, b=0.050, h=1.500, kappa=0.050
param eps=0.100, f=1.000, phi=1.000
param k=1.000, c=0.017

# Initial conditions ---------------------------------------------------------------

init P=1.0, For=0.3, J=0.8
@ total=1000, dt=0.1, bounds=1000

# plotting variables ---------------------------------------------------------------

@ XP=For, YP=P, XLO=0.1, XHIGH=8, YLOW=-0.1, YHIGH=5

# Auto options ---------------------------------------------------------------------

# Numeric settings
@ DS=0.002, DSMIN=0.001, DSMAX=0.05, NMAX=500 
# parameter ranges; the default starting  parameter for AUTO will always be the first one listed in the parameters above
@ PARMIN=-0.25, PARMAX=0.5, NORMMAX=20
# AUTO graphing settings
@ AUTOXMIN=-0.2, AUTOXMAX=0.5, AUTOYMIN=-0.1, AUTOYMAX=5

done
